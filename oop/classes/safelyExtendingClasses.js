// 1. Create a second() method in a custom array class

// Instruction:
// Extend the Array class and add a second() method that returns the second element of the array.

// ⸻

// 2. Create a removeFalsy() method in a custom array class

// Instruction:
// Extend the Array class and add a method that returns a new array with all falsy values 
// removed (e.g., false, 0, null, "", undefined, NaN).

// ⸻

// 3. Create a toSnakeCase() method in a string utility class

// Instruction:
// Add a static method to a utility class that takes a string and converts it to snake_case.

// ⸻

// 4. Create an isOdd() method in a number wrapper class

// Instruction:
// Wrap a number in a custom class and implement an isOdd() method that returns true if 
// the number is odd.

// ⸻

// 5. Create a repeat(str, times) method in a string utility class

// Instruction:
// Create a static method that repeats a given string a specified number of times.

// ⸻

// 6. Create a max() method in a custom array class

// Instruction:
// Extend the Array class and implement a method that returns the largest number in the array.

// ⸻

// 7. Create a countWords() method in a string utility class

// Instruction:
// Add a static method to count how many words are in a given sentence.

// ⸻

// 8. Create a merge(obj1, obj2) method in an object utility class

// Instruction:
// Create a static method that merges two objects and returns a new object with combined keys.

// ⸻

// 9. Create a square() method in a number wrapper class

// Instruction:
// Wrap a number and implement a method that returns the square of its value.

// ⸻

// 10. Create a unique() method in a custom array class

// Instruction:
// Extend the Array class with a method that returns a new array with duplicate values removed.

// ⸻

